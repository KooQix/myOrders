export interface Address {
    city: string;
    zip: number;
}

export interface Client {
    name: string;
    surname: string;
    phone: string;
    // address: Address;
}

export interface Operator {
    name: string;
    surname: string;
    phone: string;
}
